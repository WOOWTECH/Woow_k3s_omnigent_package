# Omnigent 0.14 升級公告草稿與 host 盤點表

> 狀態：升級後公告已定稿，待 Elmo 轉發
>
> 追蹤 issue：[WOOWTECH/Woow_k3s_omnigent_package #3](https://github.com/WOOWTECH/Woow_k3s_omnigent_package/issues/3)
>
> 維護窗口實績：2026-09-17 07:16–09:31（+08）；完成時間 09:31（+08）

`GET /v1/hosts` 只會列出目前登入者自己的 host，無法自動取得所有 host 的擁有者、CLI／tmux 版本或是否正在跑 leader。因此窗口前必須由每位 host 擁有者主動回報，下表才是升級前與升級後「全數 online」驗收的共同基線。

## Host 盤點表模板

請每位擁有者為**每一個 host** 填一列，指令輸出請原樣貼到追蹤 issue #3。`tmux` 必須至少為 3.3；「是否跑 leader」請填「是／否」，若是，T-1h 前必須停止派工並依窗口指示停掉 leader。

| 擁有者 | host 名稱 | `omnigent --version` | `tmux -V` | 是否跑 leader |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |
|  |  |  |  |  |

盤點完成條件：

- 所有已知 host 都有擁有者，且 host 名稱與 UI 中的名稱一致。
- 每台都附 `omnigent --version` 與 `tmux -V` 原始輸出。
- 所有 `tmux` 版本皆為 3.3 以上；不符合者在窗口前完成升級，但不得自行升級 omnigent CLI。
- 所有正在跑 leader 的 host 都已標記，T-1h 可逐台確認停止派工。
- 升級後用同一張表逐台確認重新 online；需手動重啟者記錄原因與處置。

## 公告草稿一：T-24h 窗口前通知

> **【Omnigent 0.14 production 升級通知｜T-24h】**
>
> 維護窗口：**TBD**
>
> 對外保留時間：**90 分鐘**
>
> 預估實際服務中斷：**單次正常路徑約 4 分鐘（依 2026-09-17 WP-7 實測）；若中止重跑，每次中斷會累加**
>
> 本次會把 production omnigent server 從 v0.12.0 升到 v0.14.0，包含資料庫 migration。窗口內 Web UI、API、leader／worker 與 host 連線會暫時中斷；server 重啟期間 host 會短暫離線並自動重連。2026-09-17 WP-7 最終成功起跑實測約 4 分鐘，但不明外部 scale 造成兩度中止重跑後，三段累計約 34 分鐘；因此仍保留 90 分鐘窗口。若 host 超過 5 分鐘仍 offline，再手動重啟 `omnigent host`。
>
> 請所有 host 擁有者在窗口前完成以下事項：
>
> 1. 到追蹤 issue #3 回報：擁有者、host 名稱、`omnigent --version`、`tmux -V`、是否正在跑 leader。
> 2. 確認 `tmux -V` **至少為 3.3**。
> 3. **T-1h 起停止所有新派工**；不要再啟動 issue-leader、repo-leader 或 worker，現有工作請在 T-1h 前收尾。
> 4. 保存尚未送出的內容。單次正常路徑約 4 分鐘不可用；若任何 gate 觸發中止重跑，前一次與新一次的中斷會累加，最壞仍以 90 分鐘窗口準備。
> 5. ⛔ **窗口前後都不要自行執行 `omnigent upgrade`**。CLI 升級必須等 maintainer 確認相容 bundle 修正已 merge 並另行放行。
>
> 維護人員會在升級前後比對 host 狀態；若你的 host 沒有自動回到 online，會在此串標註你協助重啟。進度與證據統一記在：<https://github.com/WOOWTECH/Woow_k3s_omnigent_package/issues/3>

### 發送前替換／確認

- 將 `TBD` 換成含時區的開始與結束時間。
- 確認最新一次 production 的中斷與 hosts 重連實績仍適用；若後續演練或窗口結果不同，發送前更新通知。
- 確認 host 盤點表已建立在 pkg #3，並標註尚未回報的擁有者。
- 確認 T-1h 停止派工的負責人與聯絡管道。

## 升級後公告定稿

> **【Omnigent 0.14 production 升級完成】**
>
> 完成時間：**2026-09-17 09:31（+08）**
>
> 服務狀態：**v0.14.0；`GET /health` ok；Helm test Succeeded**
>
> Host 現況：窗口驗收時 admin 可見的 6 台中 5 台 online，`woowtech-ai-coder-ASUS…` offline；其他使用者名下的 host 無法由 admin API 完整列出，因此全體盤點仍待擁有者逐台回報。
>
> Omnigent production 已升級至 v0.14.0。
>
> 本次實際服務中斷分成三段：**07:16–07:26、08:07–08:26、09:27–09:31（+08）**，累計約 **34 分鐘**，超過窗口前公告的 20–30 分鐘。原因是窗口中有來源仍待調查的自動化把 server 拉起，維護人員為了確保備份與 migration 一致性而兩度中止並從頭重做。這三段期間仍在執行的 session 可能已中斷，請回到原 session 檢查狀態並視需要重新接續，不要直接重複派工。
>
> 請每位 host 擁有者逐台確認盤點表中的機器都已回到 online，並回報「host 名稱＋online／offline」。若仍為 offline，請先不要自行重裝或 reset host ID；回覆 host 名稱，由維護人員判斷是否需要手動重啟。全部 host 對齊盤點表或留下處置紀錄後，host 驗收才算完成。
>
> v0.14 使用行為有以下變更：
>
> - **唯讀分享預設看不到 workspace 檔案。**需要分享檔案時，由 owner 在 Share dialog 明確開啟 **Workspace files**。
> - **Session 自動命名預設開啟。**快捷鍵已調整：`⌘[`／`⌘]`、`⌘⌥S`、`⌘⇧M`；請不要沿用舊版快捷鍵假設。
> - **Codex Auto 模式**的 escalated 請求不再詢問人，會依新版流程繼續；派工前請重新確認 session 的模式與預算。
> - sub-agent 若 **180 秒沒有動靜**，會被判定失敗並喚醒 parent（upstream #5856）；遇到此狀況先查看 parent／child log，不要直接重複派工。
> - ⛔ **CLI 升級仍未放行，現在不要自行執行 `omnigent upgrade`。**paas PR #1112 已 merged，但放行前還要先完成一台試點 host 的 CLI／bundle／派工驗證，並由 maintainer 明確宣布。放行時間、最低 CLI 版本與 host 應先更新到的 commit 會在本串另行通知。
>
> 窗口紀錄與驗收證據：<https://github.com/WOOWTECH/Woow_k3s_omnigent_package/issues/3>
